{
  "name": "ccdvf-exam-prep",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-html.mjs && node scripts/generate-seo.mjs && vite",
    "build": "npm run validate:questions && tsc -b && node scripts/generate-html.mjs && node scripts/generate-seo.mjs && vite build && node scripts/generate-404.mjs",
    "lint": "eslint .",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "validate:questions": "python3 scripts/validate-questions.py"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@types/node": "^22.15.0",
    "eslint": "^9.35.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.42.0",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
